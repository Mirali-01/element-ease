import React from "react";

// Future dropdown menu to display more info or highlight certain elements
// create another component for inputting elements on a search, controlled form handling and submits
// Try to use filter for the names when inputting search
const Dropdown = () => {
  return <div>Dropdown</div>;
};

export default Dropdown;
